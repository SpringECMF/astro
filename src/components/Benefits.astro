---
---
<section id="beneficios">
    <div class="container">
      <h2 class="section-title text-center">¿Qué gano con esto?</h2>
      <p class="section-subtitle text-center" style="margin-top: -1.5rem; margin-bottom: var(--s-8); opacity: 0.8;">Beneficios tangibles</p>
      
      <div class="grid-3">
        <div class="card benefit-card">
          <div class="benefit-icon">⌛</div>
          <h3>Tiempo de dirección recuperado</h3>
          <p><strong>Dejas de apagar fuegos.</strong></p>
          <p class="mt-2">Tu tiempo vuelve a estar donde importa: pensar, decidir y mover el negocio hacia delante.</p>
        </div>
        <div class="card benefit-card">
          <div class="benefit-icon">️🎯</div>
          <h3>Errores fuera de juego</h3>
          <p><strong>Procesos claros, repetibles y trazables.</strong></p>
          <p class="mt-2">Menos retrabajo, menos “¿quién tocó esto?” y auditorías que dejan de dar miedo.</p>
        </div>
        <div class="card benefit-card">
          <div class="benefit-icon">📈</div>
          <h3>Decisiones con datos</h3>
          <p><strong>Los números dejan de esconderse en hojas de cálculo.</strong></p>
          <p class="mt-2">Dashboards y alertas en tiempo real para que lo importante te encuentre a ti, no al revés.</p>
        </div>
      </div>
    </div>
</section>

<style>
    section { padding: var(--s-8) 0; }
    .text-center { text-align: center; }
    .section-subtitle { 
        color: var(--muted); 
        margin-top: -1.5rem; 
        margin-bottom: var(--s-8); 
        font-family: var(--ff-display);
        font-weight: 500;
        font-size: clamp(1.2rem, 3vw, 1.8rem);
        letter-spacing: -0.02em;
    }
    .benefit-card { 
        padding: var(--s-7); 
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }
    .benefit-card h3 {
        font-weight: 800;
        letter-spacing: -0.03em;
        font-size: 1.5rem;
        margin-bottom: 0.5rem;
    }
    .benefit-icon {
        font-size: 2rem;
        margin-bottom: 1rem;
        opacity: 0.9;
    }
    .kpi { color: var(--brand-purple); font-weight: 700; background: color-mix(in oklab, var(--brand-purple) 10%, transparent); padding: 0 4px; border-radius: 4px; }
</style>
